#include <stdio.h>
/**
* main -  main entry point
*hello c puts function
*return : always 0(success)
*/
int main (void)
{
	puts ("\"Programming is like building a multilingual puzzle");
	return (0);
}
